import React from "react"
import { Head, Main, NextScript } from "next/document"

const Layout = () => (
  <html>
  <Head>
    <title>テスト</title>
  </Head>
  <body>
  <Main />
  <NextScript />
  </body>
  </html>
)

export default Layout
